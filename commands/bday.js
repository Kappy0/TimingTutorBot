module.exports.run = async (bot, message, args) => {
	//Command will insert users and their choice of pass/fail into database

	//Second command will allow me to grab all winners and list them
}

module.exports.help = {
	name: "bday"
}